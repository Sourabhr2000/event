<h2 style="margin-left:60px;"><%= pluralize(@users.size, "User") %></h2>
<ul>
    <% @users.each do |user| %>
    <li>
        <%= link_to user.name , user %> created 
        <%= time_ago_in_words(user.created_at) %> Ago
    </li><br>
    <% end %>
</ul>